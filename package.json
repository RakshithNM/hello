{
  "scripts": {
    "buildjs": "esbuild source/js/app.js --bundle --outfile=out.js --watch",
    "buildcss": "esbuild source/css/index.css --bundle  --outfile=styles.css --watch",
    "build": "npm run buildjs & npm run buildcss"
  },
  "dependencies": {
    "esbuild": "^0.14.5"
  }
}
